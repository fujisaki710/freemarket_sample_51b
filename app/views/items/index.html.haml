.wrapper

  = render 'shared/header'

  = render 'shared/main'

  = render 'shared/aside'

  = render 'shared/footer'

  .footer-sell-btn
    = link_to "#" do
      %div 出品
      = fa_icon 'camera',class: 'icon-camera'
